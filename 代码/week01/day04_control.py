# day04_control.py ｜ 2026.9.16 周三（执行计划里周四的内容：条件判断 / 循环 / 列表推导式）
#
# 今天的规矩：
#   1. 前 15 分钟：关掉所有文件，把字频那四行默写一遍（空字典 → 遍历 → 兜底加一 → 排序取前三）
#   2. 接着 15 分钟：把昨天的变式 2、变式 3 补掉
#   3. 剩下时间：做下面的题 1-5，最后做两道主菜
#   4. 卡住 30 分钟就注释「卡住：xxx」跳过，周六回来解决
#   5. 运行方式：python3 day04_control.py
#
# 今天的核心只有两个词：
#   条件 —— 满足什么情况，就做什么事
#   循环 —— 同一件事，重复做很多遍
#   把这两个拼起来，就是编程里 80% 的逻辑。


# ==========================================================
# 题 1  条件判断：if / elif / else
#   给你一个涨跌幅，按下表打印对应的说法：
#     大于等于 5    大涨
#     大于等于 3    上涨
#     大于等于 0    微涨
#     大于等于 -3   微跌
#     其他          下跌
#
#   两个要点：
#     一是 if / elif 从上往下挨个判断，一旦命中就整段结束，后面的不再看；
#        所以顺序有讲究，先写宽的条件会把窄的条件挡死。
#     二是写完之后，把 change 改成 5、0、-3 各跑一次，
#        专门测试「刚好等于边界」的情况，这是自己给自己找 bug 的习惯。
# ==========================================================

change = 3.2

# TODO 1
if change >=5:
    print("大涨")
elif 3<= change <5:
    print("上涨")
elif 0<= change <3:
    print("微涨")
elif -3<= change <0:
    print("微跌")
else:
    print("下跌")


# ==========================================================
# 题 2  for 循环 + range + enumerate + zip
#   a) 打印 1 到 10
#   b) 打印 1 到 10 里的偶数（提示：range 的第三个参数是步长）
#   c) 从 10 倒着打印到 1
#   d) 算 1 加到 100 的总和
#      注意：这和你昨天做的字频累加是同一个模式——
#            准备一个变量存「累计结果」，每循环一次就往上加。
#            区别只是昨天累加进的是字典，今天是普通变量。
#   e) 用 enumerate 打印股票列表，前面带上序号
#   f) 用 zip 把代码列表和名字列表一对一配起来打印
#      （配对的感觉和你昨天做的 counts.items() 是一样的）
# ==========================================================

stocks = ["贵州茅台", "宁德时代", "招商银行"]
codes = ["600519", "300750", "600036"]

# TODO 2
for i in range(1,11):
    print(i)

for i in range(2,11,2):
    print(i)

for i in range(10,0,-1):
    print(i)

for i in range(1,11)[::-1]:
    print(i)

sum1=0
for i in range(1,101):
    sum1=sum1+i
print(sum1)

#   e) 用 enumerate 打印股票列表，前面带上序号
stocks = ["贵州茅台", "宁德时代", "招商银行"]
codes = ["600519", "300750", "600036"]
#卡住：enumerate 怎么用？
#参考AI
for i,name in enumerate(stocks):
    print(i,name)

#   f) 用 zip 把代码列表和名字列表一对一配起来打印
#      （配对的感觉和你昨天做的 counts.items() 是一样的）
#卡住：参考AI
stocks = ["贵州茅台", "宁德时代", "招商银行"]
codes = ["600519", "300750", "600036"]
for num,name in zip(codes,stocks):
    print(num,name)

# ==========================================================
# 题 3  while 循环
#   a) 用 while 把 1 加到 100（和题 2 的 d 小问结果应该一样）
#   b) 用 while 打印 10、8、6、4、2
#
#   必须记住的一条：while 靠条件决定停不停，
#   所以循环体里一定要有让条件变化的语句，否则就是死循环。
#   真写出死循环了，在终端按 Ctrl + C 停下来，别关窗口。
# ==========================================================

# TODO 3
#卡住：while不熟
sum=0
n=1
while n<=100:
    sum=sum+n
    n=n+1
print(sum)

#:卡住：参考AI
n=10
while n>=2:
    print(n)
    n=n-2

# ==========================================================
# 题 4  break 和 continue
#   a) 从 1 往上找，找到第一个能同时被 3 和 7 整除的数，找到就停（break）
#   b) 打印 1 到 10，但跳过所有 3 的倍数（continue）
#
#   一个字的区别：break 是「到此为止，整个循环结束」，
#                continue 是「这一次不算，回去做下一次」。
# ==========================================================

# TODO 4
#卡住：参考AI

n=1
while True:
    if n%3==0 and n%7==0:
        print(n)
        break
    n=n+1

for i in range(1,11):
    if i%3==0:
        continue
    print(i)
# ==========================================================
# 题 5  列表推导式
#   a) 生成 1 到 20 每个数的平方，放进一个列表
#   b) 挑出 1 到 50 里所有能被 3 整除的数
#   c) 把持仓列表变成 ["贵州茅台：持有", ...] 这种形式
#
#   列表推导式就是「循环 + 条件」压成一行：
#     先写「我要留下来的东西」，再写 for，需要筛就用 if。
#   它的可读性有边界——简单的一层能写，
#   一旦要写两层循环或者逻辑绕，就老老实实用普通 for 循环。
# ==========================================================
#卡住：怎么把生成的数放进列表。值得再做一次
squares=[i*i for i in range(1,21)]
print(squares)

a=[]
for i in range(1,21):
    a.append(i**2)
print(a)

for i in range(1,51):
    if i%3==0:
        print(i)

holdings = ["贵州茅台", "宁德时代", "招商银行"]
#卡住：没懂这个持仓列表啥意思
result=[]
for i in holdings:
    result.append(i+":持有")
print(result)

# TODO 5
#参考答案
squares = [i * i for i in range(1, 21)]
print(squares)

threes = [i for i in range(1, 51) if i % 3 == 0]
print(threes)

holdings = ["贵州茅台", "宁德时代", "招商银行"]
labels = [f"{name}：持有" for name in holdings]
print(labels)

# ==========================================================
# 主菜 A  打印九九乘法表
#   提示：
#     - 两层循环，外层是行（1 到 9），内层是列（1 到这一行）
#     - 内层的范围跟着外层变，这叫「内层依赖外层」
#     - print 默认每次结束都换行，用 end="\t"（制表符）能改成不换行
#     - 内层循环结束后，需要手动空一次 print() 来换行
#   期望输出（第一行只有一项，逐行变长）：
#     1×1=1
#     1×2=2   2×2=4
#     1×3=3   2×3=6   3×3=9
# ==========================================================
#没思路
# TODO 主菜 A
for i in range(1,10):
    for j in range(1,i+1):
        print(f"{j}*{i}={i*j}",end="\t")
    print()
    
        


# ==========================================================
# 主菜 B  找出 1 到 100 里的所有质数
#   质数的定义：只能被 1 和它自己整除的正整数（2、3、5、7、11……）
#   思路：
#     - 对每个数，都先假设它是质数
#     - 然后拿 2 到它自己减一之间的所有数去试除
#     - 只要碰到一个能整除的，就说明它不是质数，可以提前结束这轮判断
#     - 全部试完都没有能整除的，那就收进质数列表
#   两点要求：
#     一是不许背答案，把思路翻译成代码就算完成
#     二是自查：1 到 100 之间应该有 25 个质数
#   （进阶：其实试除到「这个数的平方根」就够了，想不明白可以留到周六）
# ==========================================================

# TODO 主菜 B
#卡住:参考答案

for i in range(2,101):
    is_prime=True
    for j in range(2,i):
        if i%j==0:
            is_prime=False
            break
    if is_prime:
        print(i)

#参考答案
primes=[]
for i in range(2,101):
    is_prime=True
    for j in range(2,i):
        if i%j==0:
            is_prime=False
            break
    if is_prime:
        primes.append(i)
print(primes)
print(len(primes))



# ==========================================================
# 收尾
#   1) 用一句话回答自己：break 和 continue 差在哪、while 和 for 各适合什么场合
#   2) 编辑 规划/两周计划-0914至0927.md，把已完成的行打勾
#   3) 明天（9.17）预读：函数 def、参数、返回值、作用域
# ==========================================================

