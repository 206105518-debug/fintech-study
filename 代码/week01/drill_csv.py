# drill_csv.py ｜ CSV 脱敏练习（不许用 csv 模块，不许看 day08）
#
# 为什么做这个文件：
#   day08 里你觉得没弄明白，其实那天真正的新语法只有一个：import csv。
#   其余全是旧知识（字典累加、循环、函数、文件、异常）凑在一起干活。
#   所以这份练习换个做法：先不用 csv 模块，自己动手把 CSV「搓」一遍，
#   你就知道 DictReader 到底替你做了什么，也就不会觉得它是黑魔法了。
#
# 规矩和类那份一样：卡住就停在那一行，用注释写下卡在哪，然后告诉我。


# ==========================================================
# 第一步（10 分钟）｜手搓一个 CSV
#   下面这段文本，本质就是一个 CSV 文件的内容：
#   第一行是表头，后面每行是用逗号分隔的数据。
#   要求（不许 import csv）：
#     1) 先按换行拆成三行
#     2) 再把每一行按逗号拆成几格
#     3) 第一行是列名，后两行是数据
#     4) 把后两行变成「字典列表」，像这样：
#        [{"代码": "600519", "名称": "贵州茅台", "收盘价": "1680.5"}, {...}]
#        提示：用表头里的列名当键，一格一格对上去
#   做完这一步你会明白：CSV 就是「逗号分隔的纯文本」，
#   而 DictReader 只是替你做了「拆行、拆格、拿表头当键」这三件事。
# ==========================================================

文本 = """代码,名称,收盘价
600519,贵州茅台,1680.5
300750,宁德时代,210.3"""

# 你的代码写在这里：



# ==========================================================
# 第二步（5 分钟）｜缺失值到底会让什么出错
#   这次只有两行有收盘价，中间那行的收盘价是空的。
#   要求：
#     a) 先什么都不管，直接把三个收盘价加起来除以 3，
#        把报错信息抄进注释里（抄完把这行代码注释掉）
#     b) 再跳过空值算，只拿两个有效值求平均
#   自查：b 的结果应该是 972.95
#   体会：缺失值不处理，程序要么崩，要么算出一个错的数——
#        这就是为什么 day08 里「跳过空值」那一步不能省。
# ==========================================================

文本2 = """代码,名称,收盘价
600519,贵州茅台,1680.5
300750,宁德时代,
002594,比亚迪,265.4"""

# a) 报错：

# b) 你的代码写在这里：



# ==========================================================
# 第三步（5 分钟）｜为什么数值列不能靠程序自动判断
#   还是上面那段数据。这次把「代码」这一列也当成数字算进去：
#     600519、300750、002594 三个值的平均是多少？
#   算出来你会发现是个三十多万的数——毫无意义。
#   因为「代码」是编号，不是数量。600519 指的不是六百，
#   002594 前面那两个零也不是数字的一部分。
#   这一步想通，你就明白为什么 day08 里要手写一张数值列名单。
#   把你算出来的结果写在下面，再去 day08 的注释里补一句话说明这件事。
# ==========================================================

# 你的代码写在这里：



# ==========================================================
# 第四步（不算代码，算想没想明白）｜五个问题，用你自己的话答
#   答不上来就空着，发给我，我们一个个拆。
#
#   1) reader 和 DictReader 差在哪？为什么我一直推荐用后者？
#   答：
#
#   2) CSV 里的数字读出来是什么类型？为什么要 float() 转一下？
#   答：
#
#   3) 缺失值为什么必须先跳过？不跳过会发生什么？
#   答：
#
#   4) 为什么数值列要手写一张名单，不让程序自动判断「能不能转成数字」？
#   答：
#
#   5) 为什么把统计逻辑封装成函数，而不是像题 4、题 5 那样平铺着写？
#   答：
# ==========================================================



# ==========================================================
# 第五步（收尾）｜把主线复述一遍
#   不看前面，按顺序写出这条流水线的四个环节：
#     文件 → __________ → __________ → __________
#   然后想一件事：十一月你搭沪深300数据管道时，
#   这条线里哪一个环节是可以省的？
#   （答案是一个都省不了，只是工具从 csv 换成 pandas。）
# ==========================================================
