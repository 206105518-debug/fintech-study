# 短期计划（2026.9.21 起，到 10.11）｜含中秋与国庆假期

> 本文件取代《两周计划-0914至0927.md》，以这份为准。
> 工作日每天 2 小时，周六 6 小时，周日 6 小时。**假期一律不排任务。**
>
> **9.21 调整记录**：9.19 周六只完成了类的前四题（`class`、`__init__`、`self`、属性、方法、多对象），
> CSV 脚本和 9.20 周日的任务顺延。下面的日程已按 9.21 起的实际情况重排，
> 三个交付物的日期同步调整：交付物 1 → 9.22，交付物 2 → 9.23，交付物 3 不变。
>
> **9.24 调整记录**：9.23 的 git 没有开工（那天的时间用在补「类」和「CSV」的手工练习上，
> 这两份练习是为解决"离开参考写不出来"专门加的，值得花）。
> git 与建仓库挪到今天 9.24，后面的 git 进阶、Linux、numpy 相应合并压缩，
> **交付物 3 的日期仍是 10.9 不变**。

---

## 一、你现在在哪（截至 9.21）

已经拿下：

- 环境搭建、跑通脚本、VS Code 与终端的使用
- Python 基础语法全套：变量与类型、字符串、列表/元组/字典/集合、条件、循环、函数、列表推导式、lambda
- 文件读写、异常处理、模块导入（9.18 完成）
- 类与对象的前四题：定义类、`__init__`、`self`、属性、方法、带参数的方法、多个对象互不影响（9.19 完成）

**还差的**：用类重写字频统计、CSV 统计脚本（交付物 1）、git 与 GitHub 仓库（交付物 2）、
Linux 命令、numpy，以及收益率与回撤脚本（交付物 3）。

---

## 二、三个交付物（这一段的验收标准）

1. 一个能读 CSV 并输出统计表的脚本 —— 原定 9.19，**顺延到 9.22 周二**
2. 一个 GitHub 仓库，代码推上去 —— 原定 9.20，**顺延到 9.24 周四**
3. 一个能算出年化收益、年化波动、最大回撤并画两张图的脚本 —— **10.9 周五，不变**

第 3 个是有意提前埋的线——这三个指标就是你十一月做多因子回测的核心评估口径。

---

## 三、日程表

> 每个学习日 2 小时；周六、周日各 6 小时。每天做完就在方框里打勾。
> 工作日那 2 小时这样分：前 40 分钟看和学，后 80 分钟动手敲。

---

### 9.19 周六（已过）｜类与对象（部分完成）

- [x] 类与对象入门——`class`、`__init__`、`self`、属性、方法（9.19 完成）
- [x] 一个类造出多个对象、改属性、带参数的方法、对象之间互不影响（9.19 完成）
- [x] 用类把字频统计封装成 `TextCounter` 类（9.21 完成）
- [ ] CSV 统计脚本 → **挪到 9.22 周二**

---

### 9.20 周日（已过）｜刷题与建仓库（未完成，任务已拆分）

- [ ] 刷题 25 题 → **挪到 10.10 周六**（和当天的 20 题合并，改成 30 题）
- [ ] 注册 GitHub、建仓库、推代码 → **挪到 9.23 周三**（和 git 连着做更顺）
- [ ] 复习与卡点整理 → **挪到 10.10 / 10.11 周末**

---

### 9.21 周一（2h）｜补完类，开工 CSV

- [x] 0:00–0:40 用类重写字频统计：写 `TextCounter`，在 `__init__` 里去掉标点并把次数统计好，再写 `total()`、`distinct()`、`top(n)`（9.21 完成）
- [x] 0:40–1:20 主菜：给它加 `once()`（只出现一次的字）和 `summary()`（一句话总结）（9.21 完成，并用两个对象验证了互不影响）
- [ ] 1:20–2:00 CSV 脚本起步：用 `csv` 模块打开 `hs300_sample.csv`，打印表头和前两行，看懂 `reader` 和 `DictReader` 的区别
- **参考**：`代码/week01/day07_classes.py`、`代码/week01/day08_csv_stats.py`

---

### 9.22 周二（2h）｜CSV 脚本收口（交付物 1）

- [x] 统计数据行数（自查 12 行）（9.22 完成）
- [x] 统计每列缺失值个数（自查：涨跌幅 3、市盈率 2、成交额 1）（9.22 完成）
- [x] 算数值列均值（自查：收盘价 209.83、涨跌幅 0.28、市盈率 22.38、成交额 26.43）（9.22 完成）
- [x] 打印成一张表，并把整件事封装成 `analyze_csv(path)`，文件不存在时不许崩（9.22 完成）
- [ ] 想清楚那个彩蛋：代码这一列看着也是数字，能不能当数值列算均值
- **今日交付**：交付物 1 —— 能一把跑通、打印出统计表的 CSV 脚本

---

### 9.23 周三（已过）｜git（未完成，挪到 9.24）

- [ ] 六个核心命令 —— **挪到 9.24 今天**
- [ ] 注册 GitHub、把 `代码/` 推上去（交付物 2）—— **挪到 9.24 今天**

---

### 9.24 周四（2h）｜git 基础 + 建仓库（交付物 2）

- [ ] 六个核心命令：`init`、`add`、`commit`、`log`、`status`、`diff`
- [ ] 写 `.gitignore`（把 `__pycache__`、`.DS_Store` 排除掉）
- [ ] 注册 GitHub、配置 SSH key、把 `代码/` 全部推上去
- [ ] 写一条像样的 commit message（说清改了什么，别写 update）
- **今日交付**：交付物 2 —— 一个能打开的仓库地址，简历上从此可以写「代码见 GitHub」
- **卡住就跳**：SSH key 配置是最容易卡住的一步，卡满 30 分钟就改用 HTTPS 方式推，先让代码上去

---

### 中秋假期（9.25 – 9.27）｜留空

三天不排任务。如果哪天手痒想学，只允许做一件事：把前面的练习重敲一遍，**不要开新内容**。

---

### 9.28 周一（2h）｜git 进阶 + Linux 命令行（合并日）

- [ ] 分支三件套：`branch`、`checkout`、`merge`；故意制造一次冲突并解决掉
- [ ] Linux 基础命令：`cd`、`ls`、`cp`、`mv`、`mkdir`、`cat`、`head`、`tail`
- [ ] `grep` 找内容、`find` 找文件、管道 `|` 与重定向 `>` / `>>`
- [ ] 动手：全程用命令行整理一遍 `代码/` 目录，这一天不用鼠标点文件夹
- **说明**：这两块都是「用」不是「学」，各一小时够；哪个没做完就挪到 10.11 复盘日

---

### 9.29 周二（2h）｜numpy 入门 + 进阶（合并日）

- [ ] 创建数组：`array`、`zeros`、`ones`、`arange`、`linspace`
- [ ] 属性：`shape` 形状、`ndim` 维度、`dtype` 类型
- [ ] 索引与切片：和列表切片有什么不同（数组能按行列两个方向切）
- [ ] 广播机制与向量化运算；把之前写过的某个 for 循环改成 numpy 的一行
- [ ] 矩阵运算：转置、点积、矩阵乘法；常用统计函数与 `axis=0` / `axis=1`
- [ ] 动手：用 numpy 生成 1000 个随机数，算均值、标准差、分位数
- **说明**：这一天内容偏多，来得及就全做，来不及就只做前四条，后两条挪到 9.30 开头

---

### 9.30 周三（2h）｜模拟收益率 + 四个核心指标

- [ ] `np.random.normal` 生成 500 天日收益率（年化收益 10%、年化波动 20%，一年按 252 个交易日折算）
- [ ] 用 `cumprod` 把日收益率累乘成净值曲线
- [ ] 累计收益率与净值曲线的关系
- [ ] 年化收益率：为什么要用 `(1 + 总收益) ** (252 / 天数) - 1`
- [ ] 年化波动率：日收益的标准差 × √252
- [ ] 最大回撤：从每一个历史高点到之后最低点的最大跌幅，用 `cummax` 一步就能算
- [ ] 动手：把四个指标写成一个小脚本，结果打印成一张表
- **自查**：最大回撤必须是负数或 0，算出正数一定是公式写反了

---

### 国庆假期（10.1 – 10.7）｜留空

七天不排任务，不开新内容。

---

### 10.8 周四（2h）｜matplotlib 基础

- [ ] 画折线图：`plot`、`xlabel`、`ylabel`、`title`、`legend`
- [ ] 保存图片：`savefig`
- [ ] 让图里能显示中文（设字体），否则中文会变成方框
- [ ] 动手：把 9.30 生成的净值曲线画出来，存成 png
- [ ] 提前画一次回撤曲线（10.9 会轻松很多）

---

### 10.9 周五（2h）｜净值与回撤两张图（交付物 3）

- [ ] 净值曲线图
- [ ] 回撤曲线图（用 `fill_between` 填色更像机构里的图）
- [ ] 把「生成数据 → 算指标 → 画图」整理成一个脚本，用函数分成几块
- **今日交付**：交付物 3 —— 收益与回撤脚本 + 两张图，放进 `代码/week02/`
- **今晚结束时的状态**：这个脚本就是你十一月多因子回测的评估模块原型

---

### 10.10 周六（6h）｜刷题 + 复习

- [ ] 3h：刷题 30 题（把 9.20 欠的 25 题一起补上），以数组、列表类的题为主
- [ ] 2h：把这一段所有语法点逐个过一遍，不熟的当场重敲
- [ ] 1h：把新脚本推上 GitHub，顺手更新 README

---

### 10.11 周日（6h）｜复盘 + 排下一段

- [ ] 2h：把所有标注「卡住」的地方集中解决一遍
- [ ] 2h：通读 `代码/` 里全部代码，给自己写一份「易错点清单」
- [ ] 2h：排下一段的任务（pandas / SQL 起步）
- **今日交付**：一份属于你自己的易错点清单——面试前你会反复看它

---

## 四、下一段预告（10.12 起）

进入全学期最重要的一段：**pandas 数据处理 + SQL 刷题 + akshare 取数**，最终交付一条沪深 300 成分股的数据管道。到时候单独出计划。

---

## 五、假期约定与保命提醒

1. **假期不排任务，是计划的一部分，不是偷懒。** 这一阶段要跑三年，节奏比爆发重要。
2. **计划滑了不用补回来，往后顺延就行。** 9.19 少做的那部分就是顺延处理的例子，不要用挤压的方式追进度。
3. **不许只看不敲。** 每个学习日动手至少占三分之二的时间。
4. **卡住 30 分钟就跳过**，注释一句「卡住：xxx」，留到周末集中解决。
5. **不要为了「准备好」而延迟动手。** 语法只学了七成就开始写脚本，剩下的三成在写的过程中补。

> 注：中秋按 9.25（周五）起放假三天、国庆按 10.1（周四）起放假七天估算。如果学校的假期安排不同，告诉我，我把后面的日程整体挪一次。

---

## 六、每天学习结束时的固定动作（2026.9.18 起）

每次学习结束后，我（陪伴者）都要输出一份**当日核心代码总结**，结构固定：

1. 当天的知识点各给一段核心代码
2. 每段代码逐行解释「它解决什么问题」
3. 常见坑与报错对照
4. 结尾三条要记住的

总结同时归档到 `规划/总结/` 下，文件名格式：`总结-MMDD-关键词.md`。
这份归档是给未来的自己看的——写脚本时速查、面试前回顾，比翻聊天记录靠谱。
